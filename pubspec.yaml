name: aligna_app
description: "Aligna – calm, premium AI coach for clarity and aligned action."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  
  supabase_flutter: ^2.5.8
  
  shared_preferences: ^2.2.3

  # State management
  flutter_riverpod: ^2.5.1

  # Localization
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # Sharing
  share_plus: ^10.0.0

  # Vibration for haptic feedback
  vibration: ^2.0.0

  # Video player for coach videos
  video_player: ^2.8.6

  # Image handling
  image_picker: ^1.1.2

  # Fonts
  google_fonts: ^6.2.1

  # Audio player for reactive aura
  audioplayers: ^6.0.0

  # iOS style icons
  cupertino_icons: ^1.0.8
  sensors_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/videos/
    - assets/coach/
    - assets/icon/
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/aligna_app_icon.png"
  min_sdk_android: 21
